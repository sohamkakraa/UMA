# Home Page Skill (`src/app/page.tsx`)

- Keep the home route lightweight and focused on routing users quickly.
- Use plain terms like "Go to dashboard" instead of technical labels.
- Avoid dense medical content on this route.
- Keep first-load performance prioritized (minimal heavy UI elements).
